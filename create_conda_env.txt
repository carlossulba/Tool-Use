1. conda create -n robotics python=3.11 -y

2. conda activate robotics

3. python -m pip install --upgrade pip

4. pip install --no-cache-dir --force-reinstall torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu128

Validate version:
python -c "import torch; print('torch', torch.__version__); print('cuda', torch.version.cuda); print('is_available', torch.cuda.is_available())"

Expected:

torch 2.7.1+cu128
cuda 12.8
is_available True

5. pip install tensordict==0.8.3 torchrl==0.8.1 --no-deps

Validate version:
python -c "import torch, torchrl, tensordict; print('torch', torch.__version__); print('torchrl', torchrl.__version__); print('tensordict', tensordict.__version__)"

6. pip install pyg-lib torch-scatter torch-sparse torch-cluster torch-spline-conv -f https://data.pyg.org/whl/torch-2.7.1+cu128.html

7. pip install torch-geometric

Validate version:
python -c "import torch_geometric; print('pyg', torch_geometric.__version__)"

8. pip install "websockets>=11"


RUN TRAINING FROM "tool_use" folder (which contains model + scenes + simulation)
python -m model.train_ppo